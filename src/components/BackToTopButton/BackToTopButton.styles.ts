import { StyleSheet } from "react-native";
import { Sizing, Outlines } from "@app/styles";

const styles = StyleSheet.create({
  button: {
    borderRadius: Outlines.borderRadius.max,
    padding: Sizing.x30,
  },
});

export default styles;
