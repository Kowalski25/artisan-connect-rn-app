import { View, Text } from "react-native";
import React, { memo } from "react";

const ThemeInput = memo(() => {
  return (
    <View>
      <Text>ThemeInput</Text>
    </View>
  );
});

ThemeInput.displayName = "ThemeInput";

export default ThemeInput;
